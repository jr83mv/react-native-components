---
name: Button
route: /Button
menu: Components
---

import { Playground, Props } from 'docz'
import Button from './Button'
import { DarkTheme } from '@edvnz/theme'

# Button

Button make basic actions more accessible and enable users to perform activities more effectively. Buttons utilize labels,size and in some cases, icons to describe the action that will take place when the user pocesses them.

### Props

<Props of={Button} table />

<Playground>
  <Button label="Sign In" size="large" />
</Playground>

### appearance

<Playground>
  <Button label="Sign In" size="large" appearance="outlined" />
</Playground>

### status

<Playground>
  <Button
    label="Sign In"
    size="large"
    status="inactive"
    style={{ marginTop: 5 }}
  />
</Playground>

### Basic Usage

```
import { Button } from '@edvnz/components`

<Button
  label="Sign In"
  size="large"
  style={{ width: "200px" }}
  appearance="outlined"
          />
```